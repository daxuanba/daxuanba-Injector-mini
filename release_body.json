# 大轩巴入库器mini v2.12

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.12）

### 新增「Steam 加速」（工具箱 → 顶部卡片）

针对国内「商店打不开 / 页面转圈 / 下载龟速」的典型场景，做了一套**完全本地、零依赖**的 hosts 优选加速：

- **测速选优**：并发向多个国内 DoH（阿里 / 腾讯 / 360 / 223.5.5.5）查询每个 Steam 域名的真实 IP，绕开被污染的系统 DNS；再对候选 IP 做 **TCP 握手 + TLS 握手 + HTTP 首包** 三段实测，自动挑最快的那条。
- **一键加速**：把优选 IP 写进 hosts 的独立标记块（`大轩巴 Steam 加速 BEGIN/END`），随后自动刷新 DNS 缓存。覆盖商店、社区、登录、结算、客服与 Akamai CDN 共 11 个关键域名。
- **一键还原**：整体移除加速块，随时恢复原状。每次改写前都会把 hosts 备份到 `userdata/hosts_backup`。
- **证书校验兜底（重点）**：优选时会**强制校验服务器证书必须匹配目标域名**。国内 `steamcommunity.com` 这类域名会被 DNS 污染解析到完全无关的 IP（实测拿到 Facebook 段的地址），不做校验就会把垃圾 IP 写进 hosts、越加速越糟。加上这道闸之后，凡是证书对不上的候选一律判死，宁可不加速也不写脏数据。
- **自动识别域名是否被污染**：如果一个域名的候选 IP 全部不可用，会直接告知「解析被污染 / 被墙，hosts 优选无效，需要本地反代型加速器」，而不是含糊地报个失败。
- **自动识别冲突的本地加速器**：如果你机器上已经在跑 **Steam 社区 302 / Steam++ / Watt Toolkit** 这类本地反代加速器（它们会把 Steam 域名解析到 `127.0.0.1` 并在本地 443 监听），程序会**点名报出进程和 PID**，并明确提醒两套方案会互相覆盖、建议二选一 —— 同时自动把回环/内网地址从候选里剔除，绝不会把 `127.0.0.1` 当成「最优 IP」写回去。
- **非管理员也能用**：检测到没有管理员权限时，加速卡片会直接给出「以管理员身份重启」按钮，一键提权后自动续上，不用手动找 exe 右键。

诊断页也顺带新增了「本地加速器占用」检查项，一眼看出 Steam 打不开到底是网络问题还是被别的加速器接管了。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- **写 hosts 需要管理员权限**，加速/还原时若权限不足会明确提示，并可用卡片上的「以管理员身份重启」提权。
- 加速只动 hosts 里的独立标记块，**不会碰你原有的 hosts 内容**（连中文注释都原样保留），一键还原即可完全回退。
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 「入库」= 写入 `addappid` 永久解锁清单；「安装」= 让 Steam 客户端下载游戏本体，两者互不冲突。
- 免费游戏页的账号信息（头像 / 余额）需登录态 Cookie，仅保存在本地 `config.json`，请自行保管、用完可清除。
