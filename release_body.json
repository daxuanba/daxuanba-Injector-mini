# 大轩巴入库器mini v2.11

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.11）

- **修复「用内置浏览器登录」点了没反应**：登录窗口构造时把内部的 Qt 桥对象当成了窗口父控件，`QMainWindow` 不接受这种类型会直接抛错，而异常发生在 Qt 槽内部会被静默吞掉 —— 表现就是按钮点了毫无反应、窗口永远不弹。现在窗口不再挂错父控件、登录用的浏览器配置由桥统一持有（登录态跨启动保留、重复点击复用同一窗口），并且**任何失败都会如实回传给界面**，同时自动降级为「系统浏览器打开登录页 + 手动粘贴 Cookie」，不会再白等。

- **新增「工具箱」页面**：
  - **Steam 错误诊断与修复**：一键体检 9 项 —— Steam 安装路径（注册表 HKCU/HKLM 多路探测）、客户端进程、目录写权限、磁盘剩余空间、入库内核、hosts 是否被屏蔽、网页缓存体积、下载残留、Steam 服务连通性。发现问题可直接点单项修复或「一键修复」（清理网页缓存 / 清理孤儿下载目录 / 注释 hosts 里的 Steam 记录 / 重启 Steam），改 hosts 前会自动备份。
  - **下载管理**：直接读 Steam 的 `appmanifest_*.acf` 状态位，列出正在下载 / 更新中的任务（带进度条、已下载/总大小、状态文案）与已安装条目，支持「移除任务」（清掉半成品缓存与清单，清单先备份到 `userdata/download_backup`，不会动已安装的游戏文件）和「打开 Steam 下载页」。

- **修复已装游戏「识别不出来」**：Steam 路径探测改为多路兜底 —— 自定义路径 → 注册表（HKCU 主 + HKLM 32/64 位）→ 常见默认安装位置（含绿色版 / 注册表被清理的情况）；扫描结果里一并返回实际扫描到的库目录，识别为空时界面直接给出排查建议，而不是只显示一句「未检测到」。

- **修复中文 Windows 下进程检测报错**：`tasklist` / `taskkill` 的输出是 GBK，之前按 UTF-8 解码会抛 `UnicodeDecodeError` 导致进程检测失效，现已按系统编码读取。

- 下载管理里把 Steamworks 运行库之类的非游戏条目单独归类，不再混在游戏列表中。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 「入库」= 写入 `addappid` 永久解锁清单；「安装」= 让 Steam 客户端下载游戏本体，两者互不冲突。
- 免费游戏页的账号信息（头像 / 余额）需登录态 Cookie，仅保存在本地 `config.json`，请自行保管、用完可清除。
- 「工具箱」里的修复动作都会先备份（hosts、下载清单），可放心点。
