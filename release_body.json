# 大轩巴入库器mini v2.7

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.7）
- **修复首页一片空白（关键）**：侧栏改版后 `flex-direction` 未覆盖旧版竖排布局，导致侧栏独占首屏、内容区被挤到屏幕外。现侧栏与内容区恢复正常左右布局，四个页面（首页 / 入库管理 / 设置 / 关于）全部修复。
- **v2.6 全部改进照常包含**：SocketIO 跨线程推送修复、清单源推荐逻辑修正、GitHub 速率校验修正等。

### v2.6 改进明细
- **修复 SocketIO 跨线程推送崩溃**：统一为单次 `SocketIO(..., async_mode='threading')` 初始化，后台线程（依赖安装 / 跑任务）的实时日志推送不再丢失或报错。
- **清单源推荐逻辑修正**：所有内置源不可用时，推荐项回退到「内置可用源」而非误选自定义源；自定义源不再被当作默认推荐。
- **GitHub 速率校验修正**：仅对真正走 GitHub API 的源（仓库类 + 自动搜索）校验额度，`steamautocracks_v2`（走 steamui 接口）不再误耗 GitHub 额度，避免无谓的「请求次数已用尽」中断。
- **类型标注修正**：创意工坊信息接口返回值元组标注与实现保持一致（5 元组）。
- **稳定性**：后台依赖安装 / 解锁器安装日志全程经 WebSocket 透传，页面进度更连贯。

## 版本说明
- **Daxuanba-Injector-mini.exe** —— 大轩巴入库器mini：黑黄主题、DX 图标、设置页分类导航、清单源折叠（点「更多」展开）、应用内打开链接、搜索增强（支持 AppID 直查 / Steam 多区域 / SteamDB / 小黑盒）、解锁器（OpenSteamTool / GreenLuma）后台自动下载安装、DLC 入库、创意工坊资源下载。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 关于页提供「打开入库器 / 创库 / 项目主页」入口，仓库与代码均已开源自维护。
