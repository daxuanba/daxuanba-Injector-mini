# 大轩巴入库器mini v2.10

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.10）
- **修复「入库管理」识别不出已装游戏**：之前用逗号一次批量查 Steam 商店详情，接口会返回 HTTP 400，导致已装游戏全部被归入"其它"而列表显示为空。现改为逐个查询 + 多库目录扫描（读 `libraryfolders.vdf`，装在 D 盘等其它库的游戏也能识别），接口失败时用 `appmanifest` 里的名字兜底。
- **修复封面图空白**：新游戏的封面在 CDN 的 hash 子目录下，旧版拼固定地址必然 404。现改为模板多路径回退 + 用商店接口取官方封面地址兜底；推荐页直接用 Steam 官方给的完整图片地址，经新增的本地图片代理 `/api/steam/proxy` 加载（仅放行 Steam CDN 域名）。
- **入库前必须登录**：未登录时点「入库」会自动拉起内置浏览器登录窗口，登录成功后继续入库；后端同样校验登录态，未登录直接拒绝（不再出现"没登账号也能入库"）。
- **新增「安装」按钮**：免费游戏 / 推荐页卡片在「入库」之外增加「安装」，调用 Steam 客户端下载安装本体；「入库管理」里的游戏卡片也加了「启动/安装」按钮，走本地服务打开 `steam://` 协议（比 webview 直接跳转可靠）。
- **新增加日志面板**：免费游戏页与推荐页新增「入库日志」，记录登录、入库、安装全过程，日志本地持久化，切到别的页面再回来不会丢。
- **界面排版修复**：免费游戏页多张卡片之前被横向压扁导致文字被裁，改为纵向排列自适应；工具栏按钮换行不再截断。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 「入库」= 写入 `addappid` 永久解锁清单；「安装」= 让 Steam 客户端下载游戏本体，两者互不冲突。
- 免费游戏页的账号信息（头像 / 余额）需登录态 Cookie，仅保存在本地 `config.json`，请自行保管、用完可清除。
