# 大轩巴入库器mini v2.8

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.8）
- **入库管理重构为「真实已装」视图**：新增「已装游戏」标签页，真实扫描 Steam `steamapps/appmanifest_*.acf`，按游戏列出已安装应用；DLC 不再单独成卡，自动归入各自父游戏下展开显示，每个游戏下清晰看到已装 DLC 列表。
- **创意工坊下载前先校验物品是否存在**：点下载前自动调用 Steam 接口检测该 ID 是否有效，不存在则直接提示并中止任务；已存在本地则提示覆盖更新，避免无效下载与「结束任务」卡死。
- **搜索模糊匹配优化**：游戏搜索去除逐字完全匹配要求，按名称相似度（包含 / 子序列 / 分词命中）重排结果，漏字、顺序不同也能命中。
- **新增「免费游戏」页（每页 20 个，可翻页）**：走 Steam 官方搜索接口（`maxprice=free`）分页抓取免费游戏，默认抓满 400 个，每页 20 个展示、可翻页；支持搜索，一键 `steam://install` 安装；可粘贴 `steamLoginSecure` Cookie 读取账号名、头像与钱包余额（仅存本地，自行保管）。
- **免费游戏「一键入库全部 / 入库本页」永久生效**：直接把 `addappid` 写进解锁器 lua 目录（SteamTools 的 `config/stplug-in` 或 OpenSteamTool 的 `config/lua`），文件保留即永久有效、重复点击自动跳过已入库项，重启 Steam 生效。
- **日志可在设置里彻底关闭**：设置页新增「关闭日志输出」主开关，开启后仅保留错误提示，安静运行。
- **手搓页独立 + 官方源**：手搓 lua 拆为独立「手搓」页，数据只走官方源（SteamCMD appinfo + Steam 商店），移除第三方密钥库依赖。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 关于页提供项目主页入口，仓库与代码均已开源自维护。
- 免费游戏页的账号信息（头像 / 余额）需登录态 Cookie，仅保存在本地 `config.json`，请自行保管、用完可清除。
