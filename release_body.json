# 大轩巴入库器mini v2.9

基于本地清单的 Steam 一键下载与入库图形工具，内嵌 Web 应用，双击即用、不调用外部浏览器。

## 本次更新（v2.9）
- **沉默浏览器登录 Steam**：免费游戏页新增「用内置浏览器登录」按钮，应用内嵌浏览器打开 Steam 登录页，登录后自动抓取登录态（不再需要手动粘贴 `steamLoginSecure` Cookie）；手动粘贴作为降级方式保留，未检测到内置浏览器时自动展开。
- **封面图本地代理**：免费游戏 / 推荐页封面图改走本地 `/api/steam/img/<appid>` 接口，多 CDN 回退 + 落盘缓存，解决 webview 直连 Steam CDN 一片空白的问题。
- **免费游戏按钮改名「入库」**：卡片按钮统一显示「入库」，点击写入 `addappid` 永久入库（不再显示「安装」）。
- **新增「游戏推荐」页**：展示 Steam 官方特惠 / 热销 / 新品 / 即将推出四个分区，卡片同样走本地封面代理，「入库」按钮一键永久入库。
- **首页 / 手搓页布局修复**：修正写死高度导致文字被裁切、首页过长不可滑动的问题，改为自适应高度 + 内容区内部滚动，整体更紧凑。
- **切页不丢任务 / 日志 / 图片**：从首页或手搓页切到其它页面再切回，后台任务继续运行，日志与进度 / 图片不丢失、不再二次刷新（服务端权威缓冲 + 本地快照合并）。
- **GitHub 镜像下载修复**：重排清单源下载镜像顺序（raw 直连 + jsDelivr 三节点 + gh-proxy.com / ghps.cc / ghproxy.cn 优先），移除已失效的 gh-proxy.org / cdn.gh-proxy.org 等（实测 522 / 404）。

## 使用
直接运行 exe，首次启动自动拉起本地服务并在窗口内加载界面；未检测到解锁工具时会在后台自动下载安装，不卡启动。

## 说明
- 清单源内置 17 个公开仓库，默认折叠前 6 个，点「更多」展开。
- 下载页资产名为 `Daxuanba-Injector-mini.exe`（ASCII，GitHub 资产名限制）；本地磁盘上的中文名为 `大轩巴入库器mini.exe`。
- 关于页提供项目主页入口，仓库与代码均已开源自维护。
- 免费游戏页的账号信息（头像 / 余额）需登录态 Cookie，仅保存在本地 `config.json`，请自行保管、用完可清除。
